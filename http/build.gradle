plugins {
    id 'java'
    id 'java-library'
}

version '1'

dependencies {
    api 'ru.odnoklassniki:one-nio:1.6.1'
    implementation project(':db')
}

jar {
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}